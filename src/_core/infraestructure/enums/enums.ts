export enum MediaType {
    IMAGE = "image",
    VIDEO = "video",
}

export enum LikeableType {
    POST = "Post",
    COMMENT = "Comment",
    // STORY = "Story" // Si también permites likes en historias
}